// This is where you'll be writing your code. The code in here will execute
// as soon as its rendered in your browser. This might cause a
// problem if the document object model (DOM) hasn't built the elements you want
// to manipulate. We need to add a 'ready' function to execute code when the
// DOM is ready to be manipulated.